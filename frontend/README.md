# Frontend (Vue + Vite)

## Setup

`ash
cd frontend
npm install
cp .env.example .env
npm run dev
`

The app expects the Flask API base URL in VITE_API_URL.

Routes:
- / public portfolio
- /admin login and content management
